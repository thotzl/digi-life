# Dependencies
node_modules/

# Production build outputs
dist/

# Local database files and save states (keep them untracked so they don't bloat the diff)
species_db.json
simulation_state.json
digilife.db
digilife.db-wal
digilife.db-shm
src-tauri/pixel_life_local.db
src-tauri/pixel_life_local.db-wal
src-tauri/pixel_life_local.db-shm

# Rust / Tauri builds
src-tauri/target/

# System / IDE specifics
.idea/
.DS_Store
*.log

# AI agent artifacts & custom cloned repositories
.agents/artifacts/
